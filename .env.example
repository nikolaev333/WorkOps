# Connection String for SQL Server
# Copy this file to .env and update with your actual values
# The .env file is ignored by git for security

# Format: ConnectionStrings__DefaultConnection
# Note: Use double underscore (__) for nested configuration keys
# Get the password from infra/docker-compose.yml
ConnectionStrings__DefaultConnection=Server=localhost,1433;Database=WorkOpsDb;User Id=sa;Password=<your-password>;TrustServerCertificate=True;
